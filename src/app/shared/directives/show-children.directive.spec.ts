import { ShowChildrenDirective } from './show-children.directive';

describe('ShowChildrenDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowChildrenDirective();
    expect(directive).toBeTruthy();
  });
});
